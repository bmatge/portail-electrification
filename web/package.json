{
  "name": "@latelier/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SPA Vue 3 — refacto v2 de L'atelier 🪢.",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -p tsconfig.app.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -p tsconfig.app.json --noEmit",
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium"
  },
  "dependencies": {
    "@gouvfr/dsfr": "^1.14.4",
    "@latelier/shared": "*",
    "axios": "^1.16.1",
    "idb-keyval": "^6.2.2",
    "pinia": "^2.3.1",
    "vue": "^3.5.34",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.4.2",
    "vue-tsc": "^2.2.12"
  }
}
