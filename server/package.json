{
  "name": "@latelier/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend collaboratif de L'atelier — outil de cadrage de sites institutionnels.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build": "echo \"[server] no build (JS legacy en Phase 0, TS en Phase 1)\"",
    "typecheck": "echo \"[server] no typecheck (JS legacy en Phase 0, TS en Phase 1)\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.1"
  }
}
